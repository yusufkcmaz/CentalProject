
| N-Katmanlı Mimari :-:
-/-/-

* Katmanlar --> Class libary trafından oluşturulmalı.
* webUI --> Web App oluşturuldu.
----
* private : Sadece bulunan sınıftan erişilir.
* protected : Sadece bulunan sınıf ve miras alınan sınıflar erişir.
* internal : Bulunduğu katmandan erişir .

----DataAccessLayaer-2---
Abstract : İnterfaceler bulunur.
concreate : İnterfacelri implament ettiğimiz sınıflar bulunur.
Repostory : Crud işlemleri gerçekleştiren kısımdır.
paket yüklemeler(EntityFrameWork) -> efcore , design , sql ,tools 
----
Enum kullanımı : entitylerimizde aynı konuda bulunan sabit verileri enum sınıfı içerisinde yazarız. veri çekme properti belirtilmez indexten erişilir.
----
SweetAlert:Uygulaması--> Areas/MySocial/Index.

sayfalama-Paged işlemi kütüphanesi: -->PagedListCoreMvc
AdminCarBrand/Index -->Paged